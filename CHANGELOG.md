# Changelog

## v0.1.1-alpha

- Added wiki support commands:
- `list-wiki-pages` for project wiki page discovery
- `read-wiki-page` for wiki metadata/text reads (v3 + legacy compatibility)
- `write-wiki-page` for wiki page create/update via legacy endpoint compatibility
- Improved wiki compatibility/error messaging for API v3 stub scenarios
- Added helper tests for API path normalization and wiki payload parsing

## v0.1.0-alpha

- Initial skill definition
- CLI wrapper with core OpenProject operations
- Weekly summary and decision logging
